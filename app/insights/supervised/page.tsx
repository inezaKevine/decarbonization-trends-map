import { SupervisedLoader } from "@/components/insights/supervised-loader"

export default function SupervisedInsightsPage() {
  return (
    <main className="min-h-screen bg-background">
      <SupervisedLoader />
    </main>
  )
}
